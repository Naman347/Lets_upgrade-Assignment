#include<iostream>
using namespace std;
int main()
{
    int n,flag=0;
    cout<<"Enter the number to check weather prime or not";
    cin>>n;
    for(int i=n-1;i>=2;i--)
    {
        if(n%i==0)
        {
            cout<<"not prime";
            flag=1;
            break;
        }
    }
    if(flag==0)
    cout<<"Prime";
    return 0;


}
